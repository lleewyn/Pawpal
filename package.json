{
  "scripts": {
    "sync": "node scripts/sync_static.js",
    "dev": "vite",
    "build": "node scripts/sync_static.js && vite build && node scripts/copy_static_dist.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  }
}
